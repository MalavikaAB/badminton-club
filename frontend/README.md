# Board prototype

Open `index.html` directly in a browser to preview the board. This temporary prototype uses sample data and will be connected to the Spring Boot API once the local Java/Node toolchain is available.
